export type NavLinks = {
	link: string;
	url: string;
}[];

export type Menu = {
	item: string;
	description: string;
	price: string;
}[];

export type Reservation = {
	url: string;
	image: string;
	buttonText: string;
}[];
